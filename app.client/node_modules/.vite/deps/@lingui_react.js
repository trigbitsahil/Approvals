"use client";
import { i as __toESM } from "./chunk-YKewjYmz.js";
import { t as require_react } from "./react.js";
//#region node_modules/@lingui/react/dist/shared/react.31c3b5fa.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var tagRe = /<([a-zA-Z0-9]+)>([\s\S]*?)<\/\1>|<([a-zA-Z0-9]+)\/>/;
var voidElementTags = {
	area: true,
	base: true,
	br: true,
	col: true,
	embed: true,
	hr: true,
	img: true,
	input: true,
	keygen: true,
	link: true,
	meta: true,
	param: true,
	source: true,
	track: true,
	wbr: true,
	menuitem: true
};
function formatElements(value, elements = {}) {
	const uniqueId = makeCounter(0, "$lingui$");
	const parts = value.split(tagRe);
	if (parts.length === 1) return value;
	const tree = [];
	const before = parts.shift();
	if (before) tree.push(before);
	for (const [index, children, after] of getElements(parts)) {
		let element = typeof index !== "undefined" ? elements[index] : void 0;
		if (!element || voidElementTags[element.type] && children) {
			if (!element) console.error(`Can't use element at index '${index}' as it is not declared in the original translation`);
			else console.error(`${element.type} is a void element tag therefore it must have no children`);
			element = import_react.createElement(import_react.Fragment);
		}
		if (Array.isArray(element)) element = import_react.createElement(import_react.Fragment, {}, element);
		tree.push(import_react.cloneElement(element, { key: uniqueId() }, children ? formatElements(children, elements) : element.props.children));
		if (after) tree.push(after);
	}
	return tree.length === 1 ? tree[0] : tree;
}
function getElements(parts) {
	if (!parts.length) return [];
	const [paired, children, unpaired, after] = parts.slice(0, 4);
	return [[
		paired || unpaired,
		children || "",
		after
	]].concat(getElements(parts.slice(4, parts.length)));
}
var makeCounter = (count = 0, prefix = "") => () => `${prefix}_${count++}`;
function TransNoContext(props) {
	const { render, component, id, message, formats, lingui: { i18n, defaultComponent } } = props;
	const values = { ...props.values };
	const components = { ...props.components };
	if (values) Object.keys(values).forEach((key) => {
		const index = Object.keys(components).length;
		if (typeof values[key] === "string" || typeof values[key] === "number") return;
		components[index] = /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, values[key]);
		values[key] = `<${index}/>`;
	});
	const _translation = i18n && typeof i18n._ === "function" ? i18n._(id, values, {
		message,
		formats
	}) : id;
	const translation = _translation ? formatElements(_translation, components) : null;
	if (render === null || component === null) return translation;
	const FallbackComponent = defaultComponent || RenderFragment;
	const i18nProps = {
		id,
		message,
		translation,
		children: translation
	};
	if (render && component) console.error("You can't use both `component` and `render` prop at the same time. `component` is ignored.");
	else if (render && typeof render !== "function") console.error(`Invalid value supplied to prop \`render\`. It must be a function, provided ${render}`);
	else if (component && typeof component !== "function") {
		console.error(`Invalid value supplied to prop \`component\`. It must be a React component, provided ${component}`);
		return import_react.createElement(FallbackComponent, i18nProps, translation);
	}
	if (typeof render === "function") return render(i18nProps);
	const Component = component || FallbackComponent;
	return import_react.createElement(Component, i18nProps, translation);
}
var RenderFragment = ({ children }) => {
	return /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, children);
};
//#endregion
//#region node_modules/@lingui/react/dist/index.mjs
var LinguiContext = import_react.createContext(null);
var useLinguiInternal = (devErrorMessage) => {
	const context = import_react.useContext(LinguiContext);
	if (context == null) throw new Error(devErrorMessage ?? "useLingui hook was used without I18nProvider.");
	return context;
};
function useLingui() {
	return useLinguiInternal();
}
var I18nProvider = ({ i18n, defaultComponent, children }) => {
	const latestKnownLocale = import_react.useRef(i18n.locale);
	const makeContext = import_react.useCallback(() => ({
		i18n,
		defaultComponent,
		_: i18n.t.bind(i18n)
	}), [i18n, defaultComponent]);
	const [context, setContext] = import_react.useState(makeContext());
	import_react.useEffect(() => {
		const updateContext = () => {
			latestKnownLocale.current = i18n.locale;
			setContext(makeContext());
		};
		const unsubscribe = i18n.on("change", updateContext);
		if (latestKnownLocale.current !== i18n.locale) updateContext();
		return unsubscribe;
	}, [i18n, makeContext]);
	if (!latestKnownLocale.current) {
		console.log("I18nProvider rendered `null`. A call to `i18n.activate` needs to happen in order for translations to be activated and for the I18nProvider to render.This is not an error but an informational message logged only in development.");
		return null;
	}
	return /* @__PURE__ */ import_react.createElement(LinguiContext.Provider, { value: context }, children);
};
function Trans(props) {
	let errMessage = void 0;
	errMessage = `Trans component was rendered without I18nProvider.
Attempted to render message: ${props.message} id: ${props.id}. Make sure this component is rendered inside a I18nProvider.`;
	const lingui = useLinguiInternal(errMessage);
	return import_react.createElement(TransNoContext, {
		...props,
		lingui
	});
}
//#endregion
export { I18nProvider, LinguiContext, Trans, useLingui };

//# sourceMappingURL=@lingui_react.js.map